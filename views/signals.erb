<div class='row'>
  <% @signals.each do |key, value| %>
      <div class='col-md-4'>
        <a href="<%= url(signal_path(key)) %>">
          <i class='fa fa-subway'></i>
          <%= I18n.t(key) %>
        </a>
      </div>
  <% end %>
</div>
